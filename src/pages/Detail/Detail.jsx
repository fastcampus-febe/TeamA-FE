import React from 'react';
import styled from 'styled-components';

const Detail = () => {
  return <DetailContainer>상세</DetailContainer>;
};

const DetailContainer = styled.div``;

export default Detail;
